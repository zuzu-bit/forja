package com.forja.app.core.navigation

import androidx.compose.runtime.Composable
import androidx.navigation.compose.NavHost
import androidx.navigation.compose.composable
import androidx.navigation.compose.rememberNavController
import com.forja.app.feature.auth.LoginScreen
import com.forja.app.feature.auth.RegisterScreen
import com.forja.app.feature.dashboard.DashboardScreen

@Composable
fun ForjaNavHost(startLoggedIn: Boolean) {
    val navController = rememberNavController()
    val startDestination: Route = if (startLoggedIn) Route.Dashboard else Route.Login

    NavHost(navController = navController, startDestination = startDestination) {

        composable<Route.Login> {
            LoginScreen(
                onLoginSuccess = {
                    navController.navigate(Route.Dashboard) {
                        popUpTo<Route.Login> { inclusive = true }
                    }
                },
                onGoToRegister = { navController.navigate(Route.Register) }
            )
        }

        composable<Route.Register> {
            RegisterScreen(
                onRegisterSuccess = {
                    navController.navigate(Route.Dashboard) {
                        popUpTo<Route.Login> { inclusive = true }
                    }
                },
                onBackToLogin = { navController.popBackStack() }
            )
        }

        composable<Route.Dashboard> {
            DashboardScreen(
                onLoggedOut = {
                    navController.navigate(Route.Login) {
                        popUpTo<Route.Dashboard> { inclusive = true }
                    }
                }
            )
        }
    }
}
