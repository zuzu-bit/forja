package com.forja.app.core.designsystem

import androidx.compose.material3.MaterialTheme
import androidx.compose.material3.darkColorScheme
import androidx.compose.runtime.Composable

private val ForjaColorScheme = darkColorScheme(
    primary = Ember,
    onPrimary = OnEmber,
    secondary = Molten,
    onSecondary = OnEmber,
    background = Coal,
    onBackground = TextPrimary,
    surface = Surface1,
    onSurface = TextPrimary,
    surfaceVariant = Surface2,
    onSurfaceVariant = TextSecondary,
    outline = BorderSubtle,
    error = Danger,
    onError = TextPrimary
)

/**
 * Tema FORJA. Aplicația este intenționat dark-only în V1 —
 * negrul industrial face parte din identitate, nu din setările sistemului.
 */
@Composable
fun ForjaTheme(content: @Composable () -> Unit) {
    MaterialTheme(
        colorScheme = ForjaColorScheme,
        typography = ForjaTypography,
        shapes = ForjaShapes,
        content = content
    )
}
